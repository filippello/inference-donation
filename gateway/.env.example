# Copy to .env and fill in. NEVER commit the real .env.
# Generate secrets with:  openssl rand -hex 24

LITELLM_MASTER_KEY=sk-admin-CHANGE-ME      # admin key: issues/revokes recipient keys
LITELLM_SALT_KEY=CHANGE-ME                 # encrypts donor credentials stored in DB
POSTGRES_PASSWORD=CHANGE-ME
